import React from 'react';
import PackageGrid from '../components/explore/v1/PackageGrid';

function ExplorePage() {
    return (
        <div style={{padding: '0 80px'}}>
        <PackageGrid />
        </div>
    );
}

export default ExplorePage;